In backend folder, create a `.env` file <br/>
Where use,<br/>
      `MDB_CONNECT` variable to connect to your online MongoDB cluster and,<br/>
       use `JWT_SECRET` variable for `your-256-bit-secret-Signature`
